# features/catalogue

Browsing and searching the accession list: filters, sort, pagination, the
specimen grid.

Empty until the catalogue is built. A feature slice owns its own components,
hooks, types and tests; anything it needs to share with another slice moves up
to `src/components`, `src/lib` or `src/types` rather than being imported across
slice boundaries.

Delete this file once the folder has real contents.
