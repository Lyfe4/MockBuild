/**
 * Placeholder home route.
 *
 * The scaffold stops here on purpose: this exists to prove the router, the
 * layout landmarks and the token layer are wired together, and to give the
 * catalogue somewhere to land.
 */
export function HomeRoute() {
  return <h1>Thornfield Botanical Archive</h1>;
}
